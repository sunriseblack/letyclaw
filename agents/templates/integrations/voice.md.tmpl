## Voice Calls

Paid calls require the owner's explicit tap-to-approve. Do not call
`voice_call` directly; prepare a `kind: "voice"` SEND trailer with the exact
E.164 phone number and task. Use `voice_call_status` only after approval, and
do not claim an outcome until the provider reports a terminal result.
